<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta charset="utf-8">
    <link rel="stylesheet" href="css/formulaire.css" type="text/css">

    <title>Connexion</title>

    <link rel="stylesheet" type="text/css" href="css/bootstrap.min.css"></link>
  </head>
  <body>
    
    <div id="container">
      <div class="jumbotron">
        <h1>Connexion</h1>
        <form action="connexion" method="POST">
          <% if (locals.wrongId) {%>
            <label>Le pseudo ou le mot de passe est incorrect, merci de ré-essayer.</label><br>
          <% } %>
          <% if (locals.wrongSession) {%>
            <label>Votre session n'est pas autorisé à acceder à cette section, merci de vous identifiez.</label><br>
          <% } %>
          <label>Pseudo : </label>
          <input type="text" name="pseudo"/> <br>
          <label>Mot de passe : </label>
          <input type="text" name="mdp"/> <br>
          <input type="submit"/>
        </form>
        <a href="inscription">Inscription</a>
      </div>
    </div>

  </body>
  <script src="js/proxyClient.js"></script>

</html>
